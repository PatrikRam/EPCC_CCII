#include <iostream>

using namespace std;

void countdown(int n)
{
    if (n ==10){
        cout<<"blastoff!";
    }
    else{
        cout<<n<<endl;
        countdown(n+1);
    }     
}    

int main(){
    int n{};
    countdown(0);
    return 0;
}
